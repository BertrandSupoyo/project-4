{
  "name": "substation-monitoring-app",
  "version": "1.0.0",
  "description": "Substation Monitoring System",
  "scripts": {
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd . && npm install && npm run build",
    "build:backend": "cd server && npm install && npm run prisma:generate",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev",
    "vercel-build": "cd server && npm install && npm run prisma:generate",
    "postinstall": "cd server && npm install && npx prisma generate"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "compression": "^1.7.4",
    "morgan": "^1.10.0",
    "uuid": "^9.0.0",
    "dotenv": "^16.0.3",
    "@prisma/client": "^6.11.1",
    "express-rate-limit": "^6.7.0"
  },
  "devDependencies": {
    "prisma": "^6.11.1",
    "nodemon": "^3.0.1"
  }
}
